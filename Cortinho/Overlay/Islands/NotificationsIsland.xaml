<UserControl x:Class="Cortinho.Overlay.Islands.NotificationsIsland"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="232" Height="168">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="HeaderText" Text="NOTIFICAÇÕES" FontFamily="{DynamicResource FontBody}" FontSize="11" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextTertiaryBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Limpar" FontFamily="{DynamicResource FontBody}" FontWeight="Medium" FontSize="12"
                       Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" Cursor="Hand"
                       MouseLeftButtonDown="ClearNotifications_Click"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ItemsControl x:Name="ItemsHost">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,6" Padding="9,7" CornerRadius="{DynamicResource RadiusSm}"
                                    Background="{DynamicResource Surface2Brush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="15" Height="15" CornerRadius="3"
                                            Background="{DynamicResource Surface3Brush}" Margin="0,1,8,0" VerticalAlignment="Top">
                                        <Image Source="{Binding Icon}" Width="12" Height="12"
                                               Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>

                                    <StackPanel Grid.Column="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Title}"
                                                       FontFamily="{DynamicResource FontBody}" FontSize="11" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="1" Text="{Binding TimeLabel}"
                                                       FontFamily="{DynamicResource FontMono}" FontSize="9.5" Foreground="{DynamicResource TextTertiaryBrush}"
                                                       Margin="6,0,0,0"/>
                                        </Grid>
                                        <TextBlock Text="{Binding Body}" FontFamily="{DynamicResource FontBody}" FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock x:Name="EmptyLabel" Text="Sem notificações." FontFamily="{DynamicResource FontBody}" FontSize="12"
                           Foreground="{DynamicResource TextTertiaryBrush}" Visibility="Collapsed"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
